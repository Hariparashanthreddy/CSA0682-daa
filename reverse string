#include<stdio.h.
#include<string.h>
int main()
{
  char str[40];
  printf("\n enter a strig to be reversed");
  scanf("%d",str);
  printtf("\n after the reverse string:%s",strrevv(str));
  return 0;
  }
